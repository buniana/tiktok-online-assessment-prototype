// iOS status bar, cropped straight from the Figma export so it is pixel-identical to the design.
export default function StatusBar() {
  return <img className="status-bar" src="/assets/status-bar.png" alt="" aria-hidden="true" draggable={false} />;
}
